---
layout: base
---

- if site.labels.length > 0
  - site.labels.each_key do |label|
    %article
      %header
        %h1
          = label
          %a{:href=>"https://issues.jboss.org/issues/?jql=project%3DCDI+AND+labels%3D#{label}", :target=>"new"}
            %span.badge.badge-info
              Issues :
              = site.labels[label].issue_count
      %p= site.labels[label].content
- else
  %p No major issues currently highlighted
